# CI Example

Simple Node.js repo with test + lint GitHub Actions.

Recent Changes
1. Workflow Updates:
   a. The GitHub Actions workflow now funs on push any branch, not just main
   b. Updated the Node.js version from 20 to 22
2. Bug Fix:
   a. Fixed a logic bug in the add.js file to ensure the add function correctly adds two numbers
